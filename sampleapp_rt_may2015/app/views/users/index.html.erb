<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>


<ul class="users">
  <%= render @users %>
</ul>
<!-- the above is the refactored version of:
<ul class="users">
  < % @users.each do |user| %>
      < %= render user %> 
  < % end %>
</ul> --->

<%= will_paginate %>
